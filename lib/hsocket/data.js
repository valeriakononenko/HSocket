

hsocket.BOOLEAN = function(buffer, opt_terminal) {
  
};

hsocket.STRING = function(buffer, opt_terminal) {
  
};


hsocket.NUMBER = function(buffer, opt_terminal) {
  
};


hsocket.ARRAY = function(buffer, opt_separators, opt_terminal, opt_length) {
  
};


hsocket.parse = function(buffer, type, opt_terminal) {
  
};
